These are SQL and mongodb projects I completed in an Introduction to File and Database Management class at UAlberta.  

#AS1
Sets up a relational database that involves users, buyers, and sellers. Essentially, it lays out how the database would be structured. 

#AS2
10 SQL queries (in increasing order of difficulty) that I solved and my thought process behind each.

#MP1
Group project that used SQL in Python to implement a library-like system with features like member profiles, penalties, login, returning and borrowing books,and searching for books in the database. 

#MP2
Reads .json file into a collection and considers optimization (such as reading in a series of batches). Includes an optimization report on which models (embedded, normalized) are better suited for which queries. 